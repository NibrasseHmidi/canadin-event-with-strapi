'use strict';

/**
 * annual-conference service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::annual-conference.annual-conference');
