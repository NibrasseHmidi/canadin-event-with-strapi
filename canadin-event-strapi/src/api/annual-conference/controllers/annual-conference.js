'use strict';

/**
 *  annual-conference controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::annual-conference.annual-conference');
